#cloud-config

hostname: ${hostname}
fqdn: ${hostname}.lab.local

manage_etc_hosts: true

users:
  - name: ansi-user
    sudo: ALL=(ALL) NOPASSWD:ALL
    groups: sudo
    shell: /bin/bash
    ssh_authorized_keys:
      - ${ssh_key}

ssh_pwauth: false