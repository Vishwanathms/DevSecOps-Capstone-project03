#cloud-config

package_update: true

packages:
  - ubuntu-desktop
  - xrdp
  - docker.io
  - git
  - curl

runcmd:
  - systemctl enable xrdp
  - systemctl start xrdp
  - useradd -m student
  - echo "student:Student@123" | chpasswd
  - usermod -aG sudo student